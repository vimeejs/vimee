name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  actions: write # upload-artifact / download-artifact
  pull-requests: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - &checkout
        uses: actions/checkout@v6

      - &setup-bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.17

      - &cache
        name: cache
        uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - &install
        name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

  build:
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *setup-bun
      - *cache
      - *install

      - name: Build
        run: bun run build

      - name: Type check
        run: bun run typecheck

      - name: Upload workspace dist
        uses: actions/upload-artifact@v4
        with:
          name: workspace-dist
          path: |
            packages/core/dist
            packages/react/dist
            packages/plugin-textarea/dist
            packages/shiki-editor/dist

  test-coverage:
    name: test-coverage (${{ matrix.package }})
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - package: '@vimee/core'
            octocov: packages/core/.octocov.yml
          - package: '@vimee/react'
            octocov: packages/react/.octocov.yml
          - package: '@vimee/plugin-textarea'
            octocov: packages/plugin-textarea/.octocov.yml
          - package: '@vimee/shiki-editor'
            octocov: packages/shiki-editor/.octocov.yml
    steps:
      - *checkout
      - *setup-bun
      - *cache
      - *install

      - name: Download workspace dist
        uses: actions/download-artifact@v4
        with:
          name: workspace-dist
          path: .

      - name: Test with coverage
        run: bun run --filter ${{ matrix.package }} test:coverage
        working-directory: .

      - uses: k1LoW/octocov-action@v1
        with:
          config: ${{ matrix.octocov }}
